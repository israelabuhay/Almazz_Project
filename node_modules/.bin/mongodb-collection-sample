#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../mongodb-collection-sample/bin/mongodb-collection-sample.js" "$@"
  ret=$?
else 
  node  "$basedir/../mongodb-collection-sample/bin/mongodb-collection-sample.js" "$@"
  ret=$?
fi
exit $ret
